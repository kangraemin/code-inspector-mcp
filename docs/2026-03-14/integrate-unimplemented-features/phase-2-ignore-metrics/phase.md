## 목표
ignore 패턴으로 이슈 필터링 적용 + metrics 모듈(complexity, coverage, duplication)을 inspect 도구에 연결

## 범위
- server.py: _should_ignore() 헬퍼 추가, ignore 패턴 필터링, metrics import 및 연결, _collect_kt_files() 헬퍼 추가

## Steps
- Step 1: ignore 패턴 필터링 적용
- Step 2: metrics 모듈을 inspect 도구에 연결
